import { IproductToDisplay } from './iproduct-to-display';

describe('IproductToDisplay', () => {
  it('should create an instance', () => {
    expect(new IproductToDisplay()).toBeTruthy();
  });
});
